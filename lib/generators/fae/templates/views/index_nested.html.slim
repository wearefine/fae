section.js-index-addedit-form
  == render 'fae/shared/index_header', nested: true

  = render 'fae/shared/nested_index_table',
    item: :<%= file_name.pluralize %>,
    cols: [<%= @form_attrs.map {|attr| ":#{attr}"}.join(', ') %>]