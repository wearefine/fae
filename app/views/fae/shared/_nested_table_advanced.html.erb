<%
  # this file is meant for backwards compatibiliy
  # DO NOT delete this partial
  # DO NOT intentially use this partial
  # use 'fae/shared/nested_table' directly
%>
<%= render 'fae/shared/nested_table',
  assoc:                assoc,
  parent_item:          parent_item,
  assoc_name:           (assoc_name ||= nil),
  title:                (title ||= nil),
  header:               (header ||= nil),
  ordered:              (ordered ||= nil),
  has_thumb:            (has_thumb ||= nil),
  cols:                 (cols ||= nil),
  assoc_name_singular:  (assoc_name_singular ||= nil),
  title_singular:       (title_singular ||= nil),
  new_path:             (new_path ||= nil),
  edit_path:            (edit_path ||= nil)
%>